<UserControl x:Class="TelemetryDashboard.UI.Controls.ScopeViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:controls="clr-namespace:TelemetryDashboard.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="#13141C">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Organic Topology Graph Overlay -->
        <controls:DataTopologyOverlay x:Name="TopologyOverlay" Grid.Row="0" Margin="4"/>

        <!-- Scope Toolbar -->
        <Border Grid.Row="1" Background="#1E1E2E" Padding="8,4" Margin="4,0,4,4" CornerRadius="6">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="BtnPause" Content="⏸️ Pause Scope" Width="110" Margin="0,0,6,0" Click="BtnPause_Click"/>
                    <Button x:Name="BtnClear" Content="🗑️ Clear Data" Width="90" Margin="0,0,6,0" Click="BtnClear_Click"/>
                    <Button x:Name="BtnAutoFit" Content="🔍 Auto Fit" Width="85" Margin="0,0,12,0" Click="BtnAutoFit_Click"/>

                    <!-- Channel toggles are generated from the channels actually received,
                         so the scope is not limited to one fixed sensor set. -->
                    <ItemsControl x:Name="ChannelToggles" VerticalAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding DisplayName}"
                                          IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                          Foreground="{Binding Brush}"
                                          VerticalAlignment="Center" Margin="0,0,10,0"
                                          Checked="Channel_CheckChanged" Unchecked="Channel_CheckChanged"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <TextBlock x:Name="ScopeStatsText" Text="Samples: 0 | Rate: 50 Hz" Foreground="#A9B1D6" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </DockPanel>
        </Border>

        <!-- ScottPlot 5 Real-Time Waveform Chart -->
        <ScottPlot:WpfPlot x:Name="MainPlot" Grid.Row="2" Margin="4"/>
    </Grid>
</UserControl>
